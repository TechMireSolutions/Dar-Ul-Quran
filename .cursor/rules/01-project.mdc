---
description: Stack, commands, env, data flow
alwaysApply: true
---

# Project

Next.js **16.2.12** · React **19.2.8** · Sanity **v6** · Tailwind **v4.3** · TypeScript **6** · Node **≥22.12** (VPS **24.17**) · RTL Urdu/Nastaliq · React Compiler **1.0**

Live: https://darulquran.pk · Runtime port **3001** → `12-production-port.mdc`  
License: **MIT** · Stack reference: **`techstack.md`**

```bash
npm run dev      # :3001, /studio — scripts/run-next.mjs
npm run build    # prebuild stubs polyfills
npm run start    # production :3001
npm run lint && npm run check:urdu && npm run test
```

## Environment

See **`.env.example`**. Never commit secrets. Security rule: **`17-security.mdc`**.

| Required | Optional (production) |
|----------|------------------------|
| `NEXT_PUBLIC_SANITY_*` | `RESEND_API_KEY`, `EMAIL_FROM`, `EMAIL_TO` |
| `SANITY_API_TOKEN` | `REVALIDATE_SECRET` |
| `NEXT_PUBLIC_SITE_URL` | `UPSTASH_REDIS_REST_*`, Turnstile keys |

## Layout

See **`14-file-structure.mdc`**. Summary:

```
app/(site)/ api/ studio/
components/{layout,ui,sections,content,seo}/
lib/{types,cache-tags,contact-schema,rate-limit,format-date,tailwind.ts,…}/
sanity/{schemaTypes,lib}/
docs/ deploy/ scripts/
```

- Thin `page.tsx`; route-private UI in `app/**/_components/`
- Shared shells: `LeafHero` · `SectionHeaderRow` · `CenteredSectionHeader` · `ListingIndexShell`
- API routes: Zod + rate limit — **`17-security.mdc`**
- CMS: `revalidate = 300` + webhook tags (`setup-revalidation` skill)
- Fetch via `get*` in `sanity/lib/fetchers.ts` (CDN in production)
- Unit tests: `lib/**/*.test.ts` (`write-tests` skill)
- Typography / RTL → **`03-rtl-urdu.mdc`** (Nastaliq leading ≥1.9, `tracking-normal`)
- Accessibility: skip link `#main-content`, touch ≥44px, visible `focus-visible`

## Key dependencies

`zod` · `resend` · `@upstash/ratelimit` (optional) · `vitest` (dev) · `babel-plugin-react-compiler`

Upgrade policy → **`13-dependencies.mdc`**. **ESLint v9** until `eslint-config-next` supports v10.
