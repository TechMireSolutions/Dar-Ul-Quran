# Static assets

CMS-driven images (logo, favicon, hero) come from Sanity. This folder holds local fallbacks only.

- Root layout uses Sanity favicon when configured; `app/icon.svg` is the Next.js file-based fallback.
