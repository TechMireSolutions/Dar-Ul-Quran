# Copilot — Dar Ul Quran

Read `AGENTS.md` and [`techstack.md`](techstack.md). Rules: `.cursor/rules/*.mdc`. Skills: `.cursor/skills/`.

Next.js 16 · Sanity v6 · RTL Urdu · `pageMetadata()` · GROQ in `queries.ts` only · `npm run check:urdu` · `npm run test`

Avoid: English UI · inline GROQ · hardcoded contacts · `rm -rf .next` while PM2 runs
